/* golden 语料：M9g 遗留转义子集（\n \t 与"2 位 \x 后接非 hex"）；改钉协议见 docs/README。
 * 用途：M9g 重写解码块时声称"旧子集产物零扰动"，但既有 9 例**无任何字符串字面量**⇒
 * 该红线当时零覆盖；本语料把遗留子集压进 sha256，使解码块的后续改动必过此闸。 */
int main(){char *s;s="a\nb\t\x41z\x9f!";return 0;}
